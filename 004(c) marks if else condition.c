#include<stdio.h>
int main()
{
	//declare variablse
	 float marks;
	 
	 printf("enter the marks");
	 scanf("%f",&marks);
	 
	 if(marks>=30)
	  {
	  	printf("%f the student is pass",marks);
	  }
	 else
	  {
	  	printf("%f the student is fail",marks);
     }
     return 0;
}
